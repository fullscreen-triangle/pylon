# Sango Rine Shumba Network Validation Framework Dependencies

# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0

# Visualization and plotting
matplotlib>=3.5.0
seaborn>=0.11.0

# Data manipulation and analysis
pandas>=1.3.0

# Asynchronous programming (built-in with Python 3.8+, but ensuring compatibility)
# asyncio - Built into Python standard library

# Path handling (built-in with Python 3.4+)
# pathlib - Built into Python standard library

# Logging (built-in)
# logging - Built into Python standard library

# JSON handling (built-in)
# json - Built into Python standard library

# Time operations (built-in)
# time - Built into Python standard library

# Mathematical operations beyond numpy
sympy>=1.9

# Hash functions and cryptographic operations (built-in)
# hashlib - Built into Python standard library

# Compression utilities (built-in)
# zlib - Built into Python standard library

# System operations (built-in)
# sys - Built into Python standard library

# Regular expressions (built-in)
# re - Built into Python standard library

# Typing support (built-in with Python 3.8+)
# typing - Built into Python standard library

# Argument parsing (built-in)
# argparse - Built into Python standard library

# Development and testing dependencies (optional)
pytest>=6.0.0
pytest-asyncio>=0.15.0
pytest-cov>=2.12.0

# Code quality tools (optional)
black>=21.0.0
flake8>=3.9.0
mypy>=0.910

# Documentation generation (optional)
sphinx>=4.0.0
sphinx-rtd-theme>=0.5.0

# Performance profiling (optional)
memory-profiler>=0.60.0
line-profiler>=3.3.0

# Jupyter notebook support (optional)
jupyter>=1.0.0
ipython>=7.0.0

# Additional scientific libraries (required for network graph analysis)
networkx>=2.6.0  # Required for hierarchical navigator graph operations
scikit-learn>=1.0.0  # For machine learning applications

# Configuration file handling (if needed for future extensions)
pyyaml>=5.4.0
configparser  # Built into Python standard library

# Progress bars for long-running validations (optional)
tqdm>=4.62.0

# Statistical analysis (optional)
statsmodels>=0.12.0

# Parallel processing utilities (built-in)
# multiprocessing - Built into Python standard library
# concurrent.futures - Built into Python standard library

# File system operations (built-in)
# os - Built into Python standard library
# shutil - Built into Python standard library

# Random number generation (built-in)
# random - Built into Python standard library
# secrets - Built into Python standard library

# Collections utilities (built-in)
# collections - Built into Python standard library

# Data serialization (built-in)
# pickle - Built into Python standard library

# String operations (built-in)
# string - Built into Python standard library

# Mathematical functions (built-in)
# math - Built into Python standard library

# Struct operations for binary data (built-in)
# struct - Built into Python standard library
